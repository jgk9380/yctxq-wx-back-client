一、新增的结构元素：
1、section
2、article
3、header
4、nav
5、footer

二、新增的块级元素
1、aside
2、figure
3、dialog

三、新增行内元素
1、mark
2、time
3、meter
4、progress

四、新增多媒体与交互元素。
1、details
2、datagrid
3、menu
4、command
5、

五、新增input元素
1、email
2、url
3、number
4、range
5、search
6、date，month，week，time，datetime，datetime-local

6、废除的元素
（1），能用css代替的元素：basefont，big，center，font，s，strike，tt，u
（2），不在使用frame废除的元素：frame，noframes
（2），只有部分浏览器支持的元素：applet，bgsound，blind，marquee



新增属性：
1、表单相关：
autocomplete ，autofocus ，form formaction，fomenctyupe，formmethod formnovalidate，formtatget
list ，min，max，step。multipte ，novalidate，pattern，placeholder，required

2、链接相关：
media，hreflang，rel，sizes，target

3、其他：
reversed，charset，type label，charset scoped async，manifest sandbox，seamless，srcdoc

